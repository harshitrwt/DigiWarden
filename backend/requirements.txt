fastapi
uvicorn
sqlalchemy
python-multipart
pydantic

# Engine + demo pipeline deps (so backend can import DigiPatron/engine)
opencv-python-headless==4.9.0.80
Pillow==10.3.0
ImageHash==4.3.1
numpy==1.26.4
scipy==1.13.0

# Optional (only needed if you add `engine/mobilenet_v2.onnx` and want semantic embeddings):
# onnxruntime>=1.17.0
